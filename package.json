{
  "name": "upbit",
  "version": "1.0.0",
  "description": "upbit 시세 조회 및 알림 봇",
  "main": "index.js",
  "scripts": {
    "port": "sudo iptables -t nat -I PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 8080",
    "start": "yarn port && cd client && yarn && yarn build && cd ../server && yarn && npx tsc && pm2 start dist/index.js",
    "dev": "cd server && npx ts-node index.ts"
  },
  "author": "JunBeum-Cho",
  "license": "MIT"
}
